/target

# Rust build artifacts for Go native FFI crate
go/native/target/

# Real compiled native libraries — never commit binaries to main branch
# CI replaces 0-byte placeholders in release tags only
go/bin/**/*.so
go/bin/**/*.dylib
go/bin/**/*.dll
