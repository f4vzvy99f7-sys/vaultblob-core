/target

# Rust build artifacts for Go native FFI crate
go/native/target/

# Go binary placeholder files (to be replaced by real CI builds)
# Remove this once CI is set up and binaries are committed
# go/bin/*/*.so
# go/bin/*/*.dylib
# go/bin/*/*.dll
